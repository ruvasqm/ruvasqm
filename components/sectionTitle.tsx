const SectionTitle = ({ title }: { title: string | JSX.Element }) => (
    <h2 className='text-4xl lg:text-7xl text-current font-neutraface font-black inline text-right'>
        {title}
    </h2>
)

export default SectionTitle
