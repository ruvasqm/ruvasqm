const SectionTitle = ({ title }: { title: string | JSX.Element }) => (
    <h2 className='text-4xl md:text-7xl text-current font-neutraface font-black inline'>
        {title}
    </h2>
)

export default SectionTitle
